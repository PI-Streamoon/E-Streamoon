create database dadosCrawler;
use dadosCrawler;

create table dados (
	id int primary key auto_increment,
    dataHora datetime,
    discoPercent double(3,1),
    ramPercent double(3,1),
    cpuPercent double(3,1)
    );
    
select * from dados order by id desc;
